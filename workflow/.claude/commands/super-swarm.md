Rolling pool parallel execution (no wave batching — launches tasks as slots free). Read the project adapter at `.claude/adapter.md` for test commands and conventions, then read and follow the super-swarm skill at `.claude/skills/super-swarm/SKILL.md`.

Use $ARGUMENTS as the plan file path. Optional: append specific task IDs to run a subset.
