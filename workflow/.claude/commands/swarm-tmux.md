Wave-based parallel execution with live tmux pane visibility — each agent runs in its own tmux pane. Read the project adapter at `.claude/adapter.md` for test commands and conventions, then read and follow the swarm-tmux skill at `.claude/skills/swarm-tmux/SKILL.md`.

Use $ARGUMENTS as the plan file path. Optional: append specific task IDs to run a subset.
