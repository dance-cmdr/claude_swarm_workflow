Dependency-aware planning for parallel execution. Read the project adapter at `.claude/adapter.md` for project context, then read and follow the swarm-plan skill at `.claude/skills/swarm-plan/SKILL.md`.

Use $ARGUMENTS as the spec path or description if provided.
