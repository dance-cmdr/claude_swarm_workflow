Design-aware parallel execution — classifies tasks as design or standard and routes them differently. Design tasks get full CLI access with design-system awareness; standard tasks get RED/GREEN TDD. Read the project adapter at `.claude/adapter.md` for test commands and conventions, then read and follow the co-design skill at `.claude/skills/co-design/SKILL.md`.

Use $ARGUMENTS as the plan file path. Optional: append specific task IDs to run a subset.
