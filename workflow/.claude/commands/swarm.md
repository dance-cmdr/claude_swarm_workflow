Parallel task execution with separated test/dev agents. Read the project adapter at `.claude/adapter.md` for test commands and conventions, then read and follow the swarm skill at `.claude/skills/swarm/SKILL.md`.

Use $ARGUMENTS as the plan file path. Optional: append specific task IDs to run a subset (e.g., `/swarm phase-a-plan.md T1 T3`).
