Specification discovery phase. Read the project adapter at `.claude/adapter.md` for project context, then read and follow the spec skill at `.claude/skills/spec/SKILL.md`.

Use $ARGUMENTS as the initial topic or feature reference if provided.
